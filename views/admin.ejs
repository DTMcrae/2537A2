<%- include("templates/header") %>

<br>
<div class="card d-flex mx-auto" style="width: 75vw;">
    <div class="card-header">
        Users
    </div>
    <form method="post" id="form" action="/statusChange">
        <ul class="list-group list-group-flush">
            <% let i = 0; %>
            <% for(i = 0; i < users.length; i++) { %>
            <%- include('templates/user', {user: users[i], id: i}) %>
            <% } %>
        </ul>
    </form>
</div>

<%- include("templates/footer") %>